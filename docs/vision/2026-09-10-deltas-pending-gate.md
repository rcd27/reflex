# Девять дельт канона — ГЕЙТ ПОЛУЧЕН 12.09.2026, канон догнал код

> **Закрыто.** Д1–Д9 внесены в `2026-09-08-reflex-one-machine-vision.md` хранителем 12.09.2026,
> каждая проверена по коду перед внесением, а не по этому письму. Документ оставлен как ЗАПИСЬ
> ДОВОДОВ: канон говорит закон, письмо помнит, чем он оплачен.
>
> Три поправки, найденные при проверке, — они и есть повод перечитать:
>
> * **Д5 гейта не потребовала: канон её уже держал.** Оговорка письма («`NfqVerdict` живы на
>   втором пути A/B, снос — отдельное решение хозяина») пережила свой предмет: второй путь снесён
>   вечером 10.09, §9.4 и §12.4 это записали. Проверено грепом: два вхождения, оба в докблоке о
>   самом сносе.
> * **Д7 внесена СЛАБЕЕ, чем просило письмо.** Письмо требовало записать законом «определён
>   идемпотентный морфизм `torn: S → S`». Такого морфизма в коде НЕТ — есть один предикат на
>   дерево (`hides_observation`) и обязанность, исполняемая каждой машиной в своём `step` и
>   доказанная мутацией. Канон говорит ровно это (§7.1), а разрыв со строгостью §9.1 назван
>   открытым вопросом (§12.8): записать морфизм значило бы завести десятую формулировку, обещающую
>   больше кода, — ровно ту болезнь, которую это письмо и лечит.
> * **Найдено сверх письма: `Резюме` показывало синтаксис ЭСКИЗА** (`engine(Backend, from(…))`),
>   вычищенный из §11 ещё 10.09 правкой Д10, — и числило закрытую §12.4 оставшейся работой.
>   Правка чинила раздел, а не предмет; у утверждения было два дома, и дома разошлись молча. Это
>   §4.1 в самом документе, и потому поправка внесена вместе с дельтами, а не отдельно.
>
> Висячая ссылка на `docs/superpowers/specs/…` снята тем же заходом: каталог с учёта снят
> (`545189f`), названо теперь то, что вынос переживает, — предмет, а не адрес файла.



*10.09.2026. Лежит рядом с каноном, а не в процессных документах: дельты — продуктовый закон,
а не запись о работе. Процессные документы (планы, спеки, отчёты) в гит не входят.*

Адресат: хранитель канона `reflex-e1`. Отправить некому (хранитель в этой сессии недоступен) —
письмо лежит здесь, чтобы его нашли.

## Расхождение осознанное

За день код прошёл девять дельт канона (Д1–Д9). Рабочие план и спека, из которых они выросли,
в гит НЕ входят — это внутренние документы процесса; всё, что обязано пережить их, собрано здесь. Все девять **предъявлены прогоном и мутацией в дереве** — тесты,
докблоки, мутационные проверки существуют и зелены. Канон (`docs/vision/2026-09-08-reflex-one-machine-vision.md`)
эти девять дельт **не отражает**: правка канона ждёт гейта хранителя, как решено планом
(«дельты канона… не вносить: они ждут гейта хранителя `reflex-e1`»).

Ниже — все девять одним списком, чтобы решение о гейте принималось по полной картине, а не по
частям, всплывавшим по ходу дня.

## Д1. §2/§7 — четвёртая буква входного алфавита

`DetectorEvent = Packet | Opaque | Tick` → `+ Torn { at }`. Отличается от `Opaque` предметом:
`Opaque` — «пакет пришёл, разобрать не смог» (незнание о содержимом); `Torn` — «пакет не пришёл,
и это известно» (знание о пропуске). Слить их значило бы объявить дыру непонятым пакетом.

Код: `core/src/detector.rs` (`DetectorEvent::Torn`), `core/tests/torn.rs`.

## Д2. §7 — клетка в исходе драйвера

`Served { Answered, Idle, Blind }` → `+ Torn`. `Idle` — работы не было; `Blind` — ждать не на чем;
`Torn` — работа была и до нас не дошла.

Код: `linux/src/queue/terminal.rs` (`Served::Torn`, `AfterRecv::Torn`).

## Д3. §9 — срок в подписи шва

`fn serve<F>(&mut self, until: Instant, decide: F) -> Served<…>`. Закон: не возвращаться раньше
`until`, кроме как с работой — ждать умеет только тот, у кого дескриптор, знать сколько — только
тот, у кого часы (§8). Пока эти двое не могли договориться, ведущий цикл заводил ожидание мимо шва
— и завёл (регресс `3202628`, закрыт).

Код: `core/src/serves.rs` (`Serves::serve`), носители в `linux/src/queue/terminal.rs`,
`windivert/src/carrier.rs`.

## Д4. §9.3 — из «спроектировано» в «построено»

Форма оказалась не той, что намечал §9.3 (`Reply { Answered, Silent }` не понадобился): ответ есть
функция буквы, потому что отвечает `Terminal::apply` на пакетной букве, а тиковая до него не
доходит. Достаточно срока и `interleave`.

## Д5. §9.4 закрывается

Три алфавита вердикта сводятся к одному функтору на стороне фасада; дубль `asked` умирает по
построению (у обобщённого `C` инхерентного `verdict` нет). `NfqVerdict`/`NfqVerdictKind` на старом
пути (nfqueue/pipeline.rs) этой работой **не** трогаются — снос второго пути A/B остаётся
отдельным решением хозяина, не выполнен и выполняться этой задачей не должен.

Код: `reflex/src/lib.rs` (`drive`, `Detecting`/`Running`/`Acting`).

## Д6. Закон `EdgeView` сужается до исполнимого

`down_bytes`/`up_bytes` — байты КАДРОВ (L3+L4+нагрузка), не нагрузки: conntrack нагрузки не знает
и знать не может, обещание было невыполнимым с рождения. Компенсация `HDR`/`FLOOR` в приборе
остаётся, но становится верной по контракту, а не по совпадению. Обещание не снимается, а сужается
— то же правило, что вчера сняло `impl CanAsk`.

Код: `core/src/edge.rs`, `instrument/src/edge_detect.rs`.

## Д7. §7 получает вторую половину: незнание — не только клетка выхода, но и ДЕЙСТВИЕ на состояние

§2 говорит о полноте входного алфавита, §7 — о клетке «не наблюдал» в выходном; закона между ними
не было. `Torn` — первая буква, которая приходит входом и обязана породить незнание на выходе.

Закон: у машины, чей выход зависит от полноты входа, определён идемпотентный морфизм `torn: S → S`,
поглощающий по свидетельству — после него ни один выход не утверждает свойство мира, свидетелем
которого было бы пропавшее наблюдение. Тождество (`torn = id`) законно только при отрицании хотя
бы одного из трёх условий подделываемости: **(а)** утверждение не о мире, а о нас; **(б)** вывод не
использует отсутствие (доказано типом или разбором осей); **(в)** отсутствие имеет независимого
свидетеля. Разобрано одиннадцать приборов, семь неверных тождеств починены, оплачено девятью
мутациями (восемь покраснели; девятая назвала мёртвый код и снята).

Побочная находка того же разбора: `ENOBUFS` несёт момент ОБНАРУЖЕНИЯ потери, не момент самой
потери — окно между ними уже принято прибором за полное. Устранить нечем, обязано быть названо
(то же сужение, что Д6).

Код: `core/src/detector.rs` (`hides_observation`), `instrument/src/*.rs` (одиннадцать приборов).

## Д8. §10 — свидетель обязан доказать способность свидетельствовать, и это касается не только тестов

§10 уже держит закон «зелёный без доказанной способности краснеть не считается» (`.certifying()`,
двумя прогонами). Тот же закон понадобился на другом носителе — на ЗАМЕРЕ: ноль `ENOBUFS` на
боевой очереди едва не был записан фактом («тревога теоретическая»), хотя означал «не измерено»
(очередь ёмкостью 1024 на ~15 пакетах в секунду не переполняется НИКОГДА).

Закон: замер без доказанной способности показать ненуль не считается, и строже — ненуль обязан
быть показан ТЕМ ЖЕ оракулом, которым потом мерят. Оплачено двумя мутантами
(`NFQA_CFG_QUEUE_MAXLEN`, `SO_RCVBUF`), разошедшимися по колонкам зеркально: `queue_dropped`
(«до очереди не дошло») и `user_dropped` («мы не забрали») — разные оракулы одного `Overrun`.
Задача 13 завела оснастку двух прогонов в стенде (`examples/detect-silent-drop/lab/mutants.sh`),
чтобы способность не приходилось доказывать заново руками при каждом будущем нуле.

Находка задачи 13 при воплощении оснастки: часть исходной сигнатуры мутанта №2 («строки `Overrun`
пошли ×8») была снята ДО задачи 8 и стала недостижимой после нее — `Overrun` перестал печататься
текстом и стал буквой `Torn`, которую приборы примера гасят молча. Оснастка сужена до счётчика
ядра; расхождение — в отчёте задачи 13.

Код: `linux/src/queue/socket.rs` (`tiny_queue_mutant`/`tiny_rcvbuf_mutant`),
`linux/src/queue/wire.rs` (`queue_maxlen_request`).

## Д9. Общий ресурс склеивает то, что канон разделил, — и делает это молча

§3 говорит, как машины КОМПОНУЮТСЯ, §4 — как область РАЗДЕЛЯЕТСЯ. Ни один не говорит, что ресурс,
общий для двух композируемых машин или двух слоёв, склеивает их обратно при зелёной сборке. За
10.09 дефект всплыл трижды в трёх местах, трижды с ценой в мире, а не в стройности.

Закон: всё, что участвует в шаге, обязано нести автора — либо позицией (произведение, §3), либо
ключом (расслоение, §4). Ресурс без того и другого — утечка между областями. Две оси:

* **(а) автор-слой, ключ, §4** — улика без адреса доставалась чужому разговору (`walk`, T8) и
  ленте §10 (буква `Event` без адреса, `2fcbb4b`); лечение — адрес едет вместе с данными;
* **(б) автор-машина, позиция, §3** — два краевых прибора делят одни 15 бит марки, произведения
  состояний нет; лечение — тип `IntoProbe::Home` объявляет ПИСАТЕЛЯ, второй писатель не собирается
  (цена прототипа: +105/−28 строк, публичный API не тронут).

Код: `reflex/src/lib.rs` (`IntoProbe::Home`, `MarkHome`, адрес улики в `drive`),
`core/src/interleave.rs` (адрес в `TapeLetter`).

## Что было нужно от хранителя (получено 12.09.2026)

Гейт на внесение Д1–Д9 в `docs/vision/2026-09-08-reflex-one-machine-vision.md` (§2, §3, §4, §7, §9,
§9.3, §9.4, §10 соответственно). Код опережает канон по всем девяти уже сегодня и предъявлен
прогоном/мутацией по каждой; канон об этом пока не знает.

## Отдельно: следствие выноса процессных документов, не дельта

**Это не Д10 и не старый долг.** Правка мелкая и техническая, но она стала нужна ровно сегодня и
только потому, что процессные документы вынесены из гита (`545189f`) — то есть это следствие уже
принятого решения, а не новое предложение о законе. Подаётся отдельно, чтобы не читалось девятой с
половиной дельтой.

**Канон ссылается на файл, которого в репозитории больше нет.** Строка 234, раздел про ct-край:

> Спека — `docs/superpowers/specs/2026-09-09-conntrack-state-edge-design.md`.

Файл цел на диске и в истории, но с учёта снят: клонировавший репозиторий по этой ссылке не найдёт
ничего. Проверено, что она единственная такая — ни одна другая строка канона, кода, манифеста или
скрипта на `docs/superpowers` не ссылается.

**Что предлагается.** Снять путь либо заменить его именем предмета («спека ct-края», без адреса
файла). Второе точнее: предмет пережил вынос, адрес — нет, и называть надо то, что переживает.

**Почему не сделано без гейта.** Канон правится только с хранителем, и «мелкая» тут не оправдание:
за 10.09 в дереве нашлось шесть формулировок, обещающих больше, чем держит код, — заводить седьмую
в самом каноне значило бы противоречить тому, что мы весь день чинили. Висячая ссылка обещает
документ; документа по адресу нет.

---

## Д10. §11 переписан по факту — и это не дельта закона, а исправление ЛЖИ документа

**Не ждёт гейта — уже внесено** (10.09.2026), потому что предмет не спорный: §11 описывал
синтаксис, которого никогда не существовало.

**Что было.** §11 — единственный раздел про публичный контракт — показывал
`engine(NFQ{queue:0}, from(TCP).filter(has_domain_name).detect(MtProto).route(FwMark(0xbb)))`,
поток показаний `notes.wire(otel::span).await` и утверждал: «`.run()` не нужен: прогон есть
потребление потока; терминатор вернул бы строителя через чёрный ход».

**Замер** (уточнён 10.09.2026 после проверки соседней сессией — первая формулировка была
неточна и опровергалась первым же грепом). `MtProto`, `FwMark`, `notes.wire`, `Stream<Notes>` —
ноль вхождений в дереве. `filter` встречается 106 раз, `route` 15 — но **ни одного как ДВЕРИ
фасада**: двери суть `about act after ask certifying detect emit engine extract from injected
observe on on_target own run secs sever unanswered unreachable why`, и ни `filter`, ни `route`
среди них нет. То есть §11 показывал операторы цепочки, которых не существует.

Прежняя формулировка «ноль вхождений» была ложной точностью — тем же родом утверждения, которое
этот документ и разбирает. Оставляю поправку видимой нарочно: утверждение, опровергаемое грепом за
минуту, не должно стоять у хранителя ни минуты.

Значит §11 — не отвергнутый вариант, а ЭСКИЗ, который не строили. Раздел, который читают ПЕРВЫМ,
потому что он про наружу.

**Что стало.** Контракт по факту: `engine(носитель).from(…).extract(…).detect(…).on|act(…).run()`.
Живые доводы прежней редакции сохранены и переобоснованы на настоящем синтаксисе («выражение, а не
строитель» — теперь с примерами того, что именно не собирается; «телеметрия не влияет на вердикт»).

**Довод против терминатора не вычеркнут, а ОТВЕЧЕН** — тремя проверяемыми доводами: потока
показаний нет без рантайма у потребителя (§9.2 понизил `Stream` до драйвера, `.await` = 0 в
крейтах морфизма); `Report` — не строитель, а значение несостоявшегося запуска (§7); конец цепочки
особое место ПО ЗАКОНУ, ибо там называется линейность ресурса (§9) и стоит гейт способности (§9.1).

**Цена названа вслух:** реакция приходит замыканием, а не значением, и §10 сверяет слова машины, но
не то, что потребитель с ними сделал. Поток показаний был бы в этом сильнее. Предел, не дефект.

## Д11. §12.4 «Замыкание движка» помечен ЗАКРЫТЫМ

Тем же движением и по той же причине: раздел числил открытым то, что закрыто сегодня. Оставлено
незакрытым честно — `NfqVerdict`/`NfqVerdictKind` живы на втором пути A/B, и снос его есть отдельное
решение хозяина, а не долг этой работы.

**Дописано вечером 10.09.2026.** Решение хозяина состоялось, второй путь снесён (`ba1aed3`), оба
типа ушли с ним: `grep NfqVerdict` даёт два вхождения, оба в докблоке о самом сносе. Оговорка выше
пережила свой предмет на несколько часов, и канон нёс её всё это время в ДВУХ местах (§9.4 и §12.4).
Снято замером, без гейта.

Стоит назвать, ЧЕМ это поймано, потому что поймано не проверкой: я пошёл править §12.4 по другому
поводу и наткнулся. Значит канон не имеет сторожа, какой есть у докблоков
(`deny(rustdoc::broken_intra_doc_links)`, `core/tests/docblock_guards.rs`), и его утверждения о коде
проверяются только тем, что кто-то мимо шёл. Это дыра процесса, не закона, — но дыра.

## Д12. Снос `reflex-engine-nfq` — пять ссылок канона, четыре по замеру и одна с половиной

Крейт снесён 10.09.2026 **по прямому решению хозяина**. Довод, решивший дело, принадлежит соседней
сессии и подаётся дословно:

> Крейт не переиспользуем не оттого, что мал, а оттого, что хранит модель, от которой мы ушли.

`Plane` — ЧЕТВЁРТЫЙ дом состояния: состояние машины живёт в ДРАЙВЕРЕ, девятнадцатью картами. Три
дома, которые фреймворк признаёт, — сам прибор (значение, возвращённое шагом), таблица по ключу
разговора, марка ядра. Оттого к крейту нельзя было написать и примера: пример показывал бы старую
модель как способ употребления нового движка.

Живое из него вынуто ДО сноса и цело: `parse`/`talk` уехали в `reflex-engine` ещё задачей 12½,
законы разбора (`parse.rs`, `unread_feeds_the_seam.rs`) — в `engine/tests/`, закон согласия
проводного и ядерного ключа (`flow_identity.rs`) — в `linux/tests/`, потому что его предмет не
разбор, а СОГЛАСИЕ двух источников, и знает обоих только тот крейт. Дерево 906 тестов, ноль падений.

Ссылок канона было пять, и они разного разряда — это важнее их числа:

| § | что стояло | разряд | сделано |
|---|---|---|---|
| §9.4 | строка `feed(…) -> Fed` в таблице «вне подписи» | **замер**: предмета нет | строка снята, «три нижние» → «две нижние» |
| §2 | `engine-nfq 0` в замере `.await` от 08.09 | **замер датирован** — истина того дня | замер цел, рядом сказано, что крейта нет, и домерен `reflex` (ноль) |
| §9.3 | путь engine-nfq/tests/unread_feeds_the_seam.rs | **новый адрес того же** | путь заменён; утверждение не тронуто |
| §9.1 | «боевой `engine-nfq` не употребляет ни одной способности» | **замер датирован** | замер цел, рядом сказано, чем разрыв закрыт |
| §12.1 | план «`Plane` берёт её» | **половина замером, половина доводом** | см. ниже |

**§12.1 — единственное место, где гейт нужен, и нужен ровно наполовину.** Что `Plane` больше не
возьмёт ключ — замер: `Plane` не существует. Правка законна без гейта, и никакой довод её не
отменит. Но план на то и план, что говорил, КУДА идти; чем заменить «первого боевого потребителя
расслоения» — довод, и он к хранителю. Оставлено названным вопросом, а не тихо вычеркнуто.

Здесь же снимается моя вчерашняя ошибка: я утверждал, что §12.1 закрыт. **Неверно.** Обе его
половины живы — ключ по-прежнему подаёт вызывающий, состояние по одному ключу `Flow` по-прежнему
лежит в двух домах. Поправила соседняя сессия; проверено в тексте, а не по памяти.

## Д13. §4 не говорит, ОТКУДА берётся ключ и можно ли ему верить

Найдено по правилу хозяина (11.09.2026): **если закон держится только на тестах — это промашка
канона, а не заслуга теста.**

Замер. §4 говорит, ЧЕМ область расслаивается («ключ и есть то, чем область расслаивается»), и
молчит о происхождении ключа: `подделать` 0 вхождений, `выдумать` 0, `доверя` 0, «клиент сообщ» 0.
Между тем ключ области `Target` для именованных целей — это SNI, то есть **имя, которое пишет сам
наблюдаемый** в своём ClientHello.

Отсюда закон, который сегодня держит ОДИН тест (`core/tests/tls_sni.rs::приманка_в_session_id_не_
уводит_разбор`) и одна фраза в докблоке `core/src/tls/mod.rs`:

> Имя цели берётся СТРУКТУРНЫМ проходом по ClientHello, а не поиском подстроки.

Цена нарушения не абстрактна. `session_id` приходит от клиента и содержит что угодно; байтовый
поиск нашёл бы там первое совпадение. Тогда **ключ области становится выдумываемым снаружи**:
всякий, кто формирует пакет, назначает себе чужую область, и копредел по цели (§4) считает не то,
что думает. Мутация проверена — наивный разбор поиском краснит оба закона теста.

Чего не хватает канону, и это ДВА разных утверждения:

1. **Ключ области выводится структурным разбором наблюдения**, а не поиском по его байтам. Разбор,
   нашедший ключ «где-то в пакете», нашёл его не там, где он объявлен форматом.
2. **Ключ, сообщённый наблюдаемым, обязан быть назван таковым.** SNI приходит от клиента; адрес и
   порт — тоже его выбор, но их подтверждает маршрут, а имя не подтверждает ничто. Сегодня канон
   этой разницы не знает, и `Named`/`Unnamed` читаются как «имя есть / имени нет», хотя настоящая
   ось — «кем сказано».

Первое — закон. Второе — возможно, новая ось у `TargetKey`, и тогда это правка §4 по существу.
Оба требуют ДОВОДА, не замера, и потому лежат здесь, а не внесены.

## Д14. §8 назвал МЕХАНИЗМ времени и не назвал ЗАКОНОВ его буквы

Найдено тем же правилом хозяина, что и Д13 («держится только на тестах — промашка канона»), но
находка другого рода: здесь канон не молчит о предмете, он говорит о нём НЕ ТО.

§8 объявляет время входной буквой и описывает шов: «сводит пакеты и узлы сетки в один **монотонный**
поток букв». Сказано верно. Не сказано — что у этой буквы есть собственные законы, и держат их
сегодня докблок модуля да три теста.

**Законы, о которых канон молчит целиком** (замер по тексту: 0 вхождений на каждый):

1. **МОНОТОННОСТЬ ЕСТЬ ТРЕБОВАНИЕ, А НЕ СВОЙСТВО, И У НЕЁ ЕСТЬ ЦЕНА.** Провод переставляет пакеты,
   очередь ядра отдаёт их не в порядке съёма, выпуск роняет часть событий — немонотонный вход есть
   НОРМА. Шов монотонизирует его зажимом: `at.max(last)`. Цена названа в докблоке
   `core/src/interleave.rs` — «опоздавшее выходит с последним выданным моментом, теряем точность на
   величину опоздания, не порядок» — и больше нигде.

   Это существенно для §8 и §10 разом. §8 читается как «буква несёт момент НАБЛЮДЕНИЯ»; на деле
   она может нести момент, подменённый ради порядка. А §10 сверяет по ленте — и лента хранит
   подменённый момент, не наблюдённый. Сторож: `a_packet_from_the_past_does_not_turn_time_backwards`.

2. **СЕТКА ОТМЕРЯЕТСЯ ОТ НАЧАЛА, А НЕ ОТ ПРЕДЫДУЩЕГО СОБЫТИЯ.** Иначе на плотном потоке (пакеты
   каждые 30 мс при окне 300 мс) пауз нужной длины нет ни одной, часы не идут ВОВСЕ, и приборы,
   чей предмет виден только при идущем трафике, молчат всегда — а молчание неотличимо от «беды
   нет». Сторож: `the_grid_is_measured_from_the_start_not_from_the_previous_event`.

3. **ВЫПУЩЕННОЕ НЕ ВЫПУСКАЕТСЯ ДВАЖДЫ.** Идемпотентность выпуска; канон слово «идемпотентно»
   употребляет, но о выпуске не говорит. Сторож: `what_was_released_is_not_released_again`.

**Почему это дельта, а не докблок.** Все три — свойства ВХОДНОГО АЛФАВИТА, то есть предмет §2 и §8,
а не деталь реализации шва. Второй носитель (запись pcap) уже показал, что они не теоретические:
у записи своя эпоха, и сетка, отмеряемая от предыдущего события, дала бы на ней другой ответ, чем
на живой очереди. Закон, который меняет ответ при смене носителя, обязан стоять в каноне, а не в
докблоке одного модуля.

Правится ДОВОДОМ (это добавление законов, не исправление лжи) — потому в очередь, а не внесено.

## Замечание к самому гейту

Д10 и Д11 внесены БЕЗ ожидания гейта, и это осознанное отступление от правила «канон правится только
с гейтом хранителя». Причина: гейт существует, чтобы спорное не проходило молча, а здесь спорного
нет — документ утверждал несуществующее, и всякий день промедления есть ещё один читатель, узнавший
публичный контракт, которого нет. Дельты Д1–Д9 гейта ЖДУТ, потому что они меняют ЗАКОН; Д10 и Д11
приводят описание в соответствие с уже принятым.

**Критерий, чтобы прецедент не расползся** (сформулирован соседней сессией, принимаю дословно):

> Канон правится БЕЗ гейта, когда его утверждение опровергается ЗАМЕРОМ — грепом, сборкой,
> прогоном. Правится С гейтом, когда для опровержения нужен ДОВОД.

Первое — вопрос факта, и починка факта не есть законодательство. Второе — вопрос закона, и там
гейт по существу. Д1–Д9 требуют довода, потому и ждут; Д10–Д11 опровергались грепом за минуту.
Без записанного критерия следующий решал бы по ощущению «спорно / не спорно».

Если хранитель сочтёт иначе — правка снимается одним `git revert`, и это дешевле, чем ещё день
ложного контракта в разделе, который читают первым.
