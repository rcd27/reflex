# Состояние — в IO-край: conntrack как носитель слоя разговора

> Дизайн-документ. 2026-09-09. Опирается на канон
> `docs/vision/2026-09-08-reflex-one-machine-vision.md`.

## Решение

Состояние приборов уезжает из юзерспейса в **conntrack ядра**. Слой
разговора (`Base for Conversation { type Fibre = Flow }`, §4) и слой
conntrack — один и тот же слой, индексированный одной и той же
четвёркой; совпадение не аналогия, а определение. Отсюда:

- ядерный вид разговора (`CtView`) входит **расслоением носителя**:
  `DetectorEvent<(Wire, CtView)>`, сужение — существующими `Reads`/`lmap`;
- следующее состояние — **слово области `Conversation`**, едущее в ядро
  спуском `Packet: Within<Conversation>` вместе с вердиктом;
- юзерспейс становится zero-state: собственное `S` прибора вырождается
  в `()`, то есть машина Мили с единичным состоянием (§1, законно без
  нового имени).

Носителем служит **свой netlink-сокет к `NFNL_SUBSYS_QUEUE`**; крейт
`nfq` с этого пути срезается.

## Закон и его носитель — не одно и то же

**Закон носитель-независим:**

> Состояние машины живёт на крае. Юзерспейс zero-state: собственное `S`
> прибора — `()`, а слой разговора хранит край, который этот слой и так
> ведёт.

Всё, что ниже про `NFQA_CT`, `CTA_MARK` и `ctnetlink`, — **один носитель
этого закона**, годный ровно для очереди netfilter. Тем же законом
встают другие края: карта eBPF/XDP, состояние в чужой ОС. Меняется, кто
чеканит букву и куда уезжает слово; не меняются ни `S = ()`, ни адрес
слова (`Conversation`), ни закон одного чеканщика.

Практическое следствие для формулировок: conntrack-специфике запрещено
протекать в общий закон. `CtView` — вид КРАЯ, а не «вид conntrack»; поля
его — величины, которые край ведёт (сколько прошло в каждую сторону, как
давно, в каком состоянии по мнению края), а не имена атрибутов netlink.
Способность доказывается носитель-независимо; `certify::remembering`
формулируется над `Terminal`, а не над очередью.

## DoD

1. `cargo test --workspace` зелёный.
2. Девятый закон `certify::remembering` держится на живом ядре в
   `linux/examples/certify.rs`: отданное ядру состояние вернулось
   следующим шагом того же потока, чужие биты марки целы.
3. В горячем пути нет **класса** запрос-ответ: между приёмом пакета и
   вердиктом не делается ни одного системного вызова, читающего ядро в
   ответ на наш запрос (ни `CT_GET`, ни `getsockopt`). Марка на запись
   уже приехала в `NFQA_CT`, изменение — в юзерспейсе, чтения перед
   записью нет. Проверяется прогоном, не чтением кода: `strace -c` на
   боевом стенде под нагрузкой показывает только `recvmsg`/`sendmsg`.
   Счёт 1:1 к пакетам критерием НЕ является и честно нарушается
   собственной спекой: приём отдаёт несколько сообщений одним буфером
   (`recvmsg` меньше, чем пакетов), а `ENOBUFS` даёт `recvmsg` без
   пакета вовсе.
4. `FlowTable` из фасада ушёл: приборы не заводятся на ключ.
5. Предпосылки машины проверены до старта, а не обнаружены молчанием.

## Что ядро уже считает за нас

Ядро при флаге `NFQA_CFG_F_CONNTRACK` прикладывает к каждому пакету
очереди атрибут `NFQA_CT` (`ctnetlink_glue_build`). Приезжает даром:

| величина | атрибут | чем была бы у нас |
|---|---|---|
| пакеты и байты по направлениям | `CTA_COUNTERS_ORIG/REPLY` | два счётчика на поток |
| возраст потока | `CTA_TIMESTAMP` | момент первого пакета |
| время с последнего пакета | `база(состояние) − CTA_TIMEOUT` | момент последнего |
| TCP-состояние по мнению ядра | `CTA_PROTOINFO` | свой автомат TCP |
| четвёрка, идентификатор | `CTA_TUPLE_ORIG`, `CTA_ID` | ключ таблицы |
| наше состояние | `CTA_MARK` | поле записи таблицы |

`CTA_TIMEOUT` — остаток жизни записи, и `__nf_ct_refresh_acct`
переставляет его на `now + timeouts[state]` при каждом пакете. Значит
«сколько прошло с последнего пакета» — ядерная величина, разрешение
1 с (`nf_ct_expires(ct) / HZ`). База берётся из
`net.netfilter.nf_conntrack_tcp_timeout_*` по состоянию из `CTA_PROTOINFO`.

Обратно из вердикта ядро принимает (`ctnetlink_glue_parse_ct`):
`CTA_MARK`, `CTA_LABELS`, `CTA_STATUS`, `CTA_TIMEOUT`, `CTA_HELP`.

**Третий предел, найденный при реализации приборов (16:25, Task 7).** `idle` — это простой ПОТОКА, а не направления: ядро переставляет таймаут на ЛЮБОМ пакете, включая ретрансмит клиента. Значит окно тишины по `idle` не сработает ровно там, где беда живее всего: цель ответила пару пакетов и замолчала, клиент повторяет — таймаут освежается повтором, `idle` близок к нулю, прибор молчит.

Отсюда честная область прибора тишины на ядерных величинах: он ловит «не ответила ВОВСЕ» (`up_packets == Some(0)`, счётчик ядра) и «поток простаивает целиком» (`idle`), но НЕ ловит «замолчала посреди разговора при активном клиенте». Этот случай выражается не тишиной, а неподвижностью счётчика ответов при растущем счётчике запросов — то есть прибором повтора (`Retransmit`), где оттиск и работает точкой отсчёта.

Практическое следствие: **оттиск нужен прибору ПОВТОРА, а не прибору тишины.** В памятке он лежит для обоих (памятка одна), но решение по нему принимает повтор.

**Чего ядро не даёт:** тишины НАПРАВЛЕНИЯ. Клиент повторяет — `timeout`
освежается, `reply.packets` стоит. Заметить «стоит» нечем, кроме точки
отсчёта; она и есть единственное, что мы кладём в марку сверх фазы.

## Граница: своя дверь к очереди

Крейт `nfq` (в дереве `0.2.5`, опубликован 2022-01-26) на этом пути
срезается. Причина не в возрасте: в `master` апстрима `parse_ct_attr`
разбирает единственный атрибут `CTA_ID`, а сборка вердикта кладёт
`NFQA_VERDICT_HDR | NFQA_MARK | NFQA_PAYLOAD` и не кладёт `NFQA_CT`.
Обе нужные способности лежат непринятыми PR с 2023 года (#14 «mark
conntrack», #17 «try_verdict»). Ждать апстрим — ждать чужую очередь
ревью.

Новый модуль повторяет раскрой, уже оплаченный в `linux/src/conntrack/`:
**IO отдельно, разбор отдельно**, чтобы разбор проверялся без root.

| шаг | сообщение | зачем |
|---|---|---|
| открытие | `NFQNL_MSG_CONFIG` / `CMD_BIND` | привязка к очереди |
| открытие | `CFG_PARAMS` | copy-packet, диапазон |
| открытие | `CFG_FLAGS` / `NFQA_CFG_F_CONNTRACK` | **включает `NFQA_CT`** |
| приём | `NFQNL_MSG_PACKET` | `PACKET_HDR`, `PAYLOAD`, `MARK`, `CT`, `CT_INFO` |
| ответ | `NFQNL_MSG_VERDICT` | `VERDICT_HDR` + `NFQA_CT{CTA_MARK}` |

Атрибуты внутри `NFQA_CT` — те же `CTA_*`, что разбирает
`conntrack/wire.rs`: `attrs`, `tupled`, `counted`, `entry_of`
переиспользуются, меняется только источник тела.

**Закон одного чеканщика.** `CTA_*` разбирает ровно один код —
`conntrack/wire.rs`. Горячий путь обязан пойти через него, а не отрастить
свой разбор той же марки: conntrack — единственный источник истины, и
два разбора одного `CTA_MARK` разошлись бы молча при зелёной сборке.
Дверей к стору может быть сколько угодно; чеканщик буквы — один.

Вместе с крейтом уходит добывание дескриптора разницей множеств
`/proc/self/fd` (`nfqueue/backend.rs`) и вариант `Waited::Blind`:
свой сокет — свой fd.

Приходит на нас то, что крейт делал молча: `ENOBUFS`, несколько
сообщений в одном буфере, GSO-пакеты. Это цена, и она сертифицируется
на стенде, а не тестом.

## Алфавит: расслоение носителя

Носитель `T` в `DetectorEvent<T>` живёт только в варианте `Packet`
(`Tick { node }`, `Opaque { why }` носителя не несут по построению).
Поэтому ct-вид кладётся в носитель:

```
DetectorEvent<(Wire, CtView)>
```

и присутствует ровно там, где есть буква провода — обе половины из
одного `NFQA_CT`-пакета. Ни `Option`, ни отдельного слоя.

Сужение — существующая фибрация §4, не новый комбинатор: прибор провода
проецирует `.0`, ядерный прибор `.1`, смешанный берёт пару. Существующие
приборы остаются объявленными на `Seen`/`SeenTcp`; меняется их `Reads`,
не логика.

Расширение поля внутри `Packet` **отвергнуто**: сузить можно множитель
произведения, но не поле — фибрация сломалась бы.

```
CtView {
    id, tuple: Tuple, orig: Counts, reply: Counts,
    since: CtStamp, idle: CtIdle, tcp: CtProto, state: CtState,
}
```

`Counts` и `Tuple` — существующие типы `conntrack::wire`.

**Доступность привязана к носителю типом, а не проверкой.** `CtView`
входит в широкое слово только у очереди; прибор, читающий ядерный вид,
не соберётся с носителем, у которого такого `impl Reads` нет.
Запрещать нечего — достаточно не давать `impl` там, где ядра нет.

## Слово: состояние адресовано разговору

`NFQA_MARK` — слово о **пакете**: живёт до конца обхода. `CTA_MARK` —
слово о **разговоре**: живёт весь поток. Отсюда `CtState::Of =
Conversation`, и выход прибора

```
Out = (Distress, CtState)        // обе Of = Conversation
```

проходит закон пары (`word.rs:160`: `impl<A: Word, B: Word<Of = A::Of>>`)
без новых сущностей. Вариант с `Of = Packet` закон пары не принимает:
`Distress::Of = Conversation`, а разным областям слиться нельзя.

На край слово едет **спуском** (`Packet: Within<Conversation>`,
`Descends`, §5): состояние принадлежит потоку, но уезжает в ответе на
конкретный пакет, где встречается с вердиктом — и там обе половины уже
`Of = Packet` и склеиваются законом пары в единое ядерное слово,
физически отдаваемое одним `NFQNL_MSG_VERDICT`.

Комментарии баз ложатся на физику: `Packet` — «ждать нельзя: очередь
держит его до ответа»; `Conversation` — «ожидание терпит».

## Кодек марки

```
тег       4 бита   подпись писателя (константа, ненулевая) — младшие биты маски
фаза      3 бита   ничего не сказано / подозреваем / подтвердили / отпустили
оттиск    8 бит    младшие биты reply.packets на момент постановки фазы
```

Тег внизу: он читается первым и решает, доверять ли остальному. Ненулевой —
иначе нетронутое ядром слово читалось бы как наше с пустой фазой.

Маска — **параметр цепочки**, не константа фреймворка. Фреймворк не
знает ни о каком конкретном соседе по машине.

**Запись — read-modify-write под маской.** Читаем полное слово из
`NFQA_CT`, меняем свои биты, кладём полное обратно. Чужая разметка
переживает наш шаг, даже если мы о ней не знаем.

**Расхождение — буква входа, не показание.** Тег писателя даёт проверку
без памяти: `tag ≠ наш` означает, что по нашим битам писал другой агент.
Сравнение с константой, не с запомненным значением, — иначе zero-state
рушился бы об собственную проверку. Клетка `CtView::Diverged { ours,
theirs }` заселена по образцу `Sensed::Unread` (§7): улика, на которую
прибор вправе среагировать, имеет право быть увиденной, значит идёт во
вход. Приборы без ct-вида проецируют мимо и не платят.

Оттиск лежит в ядре по той же причине, что и фаза: у себя он был бы
таблицей, растущей с числом потоков. В ядре он переживает наш рестарт —
подозрение, поставленное до падения демона, продолжает наблюдаться с
той же точки.

**Предел этого свойства: оно про детекцию, не про адресацию.** Таблица
имён юзерспейсная, и рестарт её обнуляет. Поток с ядерным подозрением
после подъёма демона продолжит наблюдаться и выстрелит, а имени у него
не будет: рукопожатие позади, нового `ClientHello` не случится, и цель
опознается адресом. Имя переучивается только с новой личности — тот же
корень, что у порядка отказа в разделе о живости.

`CTA_LABELS` — запасной носитель, не основной: пятнадцать бит помещаются
в марку с запасом, а labels требуют ненулевого их размера в ядре.
Ширина полей — умолчание, а не закон: маска задаётся снаружи, и три поля
переносятся в labels без изменения остального, если места в марке не
дали.

## Тишина: третьей двери не нужно

Первая редакция вводила «холодный канал» — дамп conntrack по тику для
приборов, срабатывающих в тишине. **Отвергнуто**: §2 говорит, что войти
в шаг можно только буквой входного алфавита, и дамп на тике был бы
третьей дверью.

Развязка проще и лежит в самой задаче: **тишина цели наблюдается на
пакетах клиента.** Цель молчит — клиентское ядро ретрансмитит с
нарастающим таймаутом, и на каждом повторе мы шагаем, имея свежий
`NFQA_CT`.

**Предпосылка, без которой это неверно, и она сужает область.** Развязка
работает, только пока беда **client-serving**: кому-то прямо сейчас
нужен ответ, и его ядро об этом напоминает. Отсюда два известных предела.

*Предел по окну.* Окно детекции обязано быть короче упорства клиентских
повторов. Пять секунд укладываются в первые повторы; окно в минуту при
клиенте, сдавшемся на тридцатой секунде, попадёт в слепое пятно: запись
в ядре ещё жива (established-таймаут по умолчанию — сутки), пакетов уже
нет, беда реальна, а высказать её не на чем.

*Предел по направлению дропа.* Режут запрос — сервер его не увидел, ACK
не пришёл, клиент повторяет, буквы идут: покрыто. Режут **ответ** —
`ClientHello` доставлен и подтверждён, повторять клиенту нечего, повторы
сервера дропаются до нас. Пакетов на нашей двери нет, хотя беда именно
client-serving. Кончится это клиентским `FIN` по таймауту приложения, а
`Seen::Closed` переводит наблюдение в `Watch::Ended` — прибор не
выскажется вовсе.

То есть на одном горячем крае `reflex` ловит дроп запроса надёжно, дроп
ответа — только если клиент переоткроет соединение и на новом потоке
срежут уже запрос.

**Граница не измерена, и это записано как незакрытое.** Какой из двух
режимов преобладает на вантаже — вопрос замера, а не рассуждения. Оба
физически возможны: inline-фильтр читает `ClientHello` и вправе уронить
ЕГО ЖЕ (чтение не требует доставки) — это дроп запроса, и именно он
наблюдался на `rutracker` по ретрансмитам клиента; он же вправе
пропустить запрос и уронить ответ. Замер входит в сертификацию: есть ли
на вантаже цель, у которой `ClientHello` подтверждён, а `ServerHello`
сброшен, и молчит ли на ней новый путь. Если такие цели окажутся частыми,
холодный край выходит из YAGNI в обязательные.

**Что возвращает холодный край.** Любой из трёх триггеров: окно длиннее
горизонта клиентской активности; требование ловить дроп ответа в том же
потоке; архивный потребитель, которому беда нужна ПОСЛЕ того, как клиент
сдался (собрать список дропнутых целей — беда не client-serving по
определению). Тогда оживает развилка «точечный `CT_GET` по кортежу
против `NLM_F_DUMP`» (O(живых) сисколлов против O(таблицы) разбора) и
подписка на `DESTROY`.

Отдельно названо, чего в этом отказе НЕ было: §2 холодному краю не
препятствует. Запрещён скрытый вход **в шаг**, а не поход **драйвера** в
мир — «недетерминизм живёт в драйвере, за дверью буквы», и замер §2
разрешает `.await` драйверам, запрещая морфизму. Драйвер вправе сходить
в ядро и отчеканить букву, ровно как `Interleave` чеканит тик из своих
часов. Холодный край не строится **по YAGNI под client-serving область**,
а не по запрету.

Тик остаётся тем, чем был (§9.3, тишина обязана доходить), и ядерного
вида не несёт. Приборы, которым нужен ядерный вид, работают по букве
`Packet`.

Push-канал ctnetlink (`NFNLGRP_CONNTRACK_DESTROY`) назван и **не
строится**: второй сокет и вторая машина событий ради случая, которого
в client-serving области нет.

**Форма холодной буквы — открытый вопрос.** Носитель живёт только в
`Packet`; холодной букве нужен вид края без провода, а вешать его на
`Tick` — та же поломка, что расширение поля. Похоже, алфавиту нужна
четвёртая буква: «пакета не было, но край опрошен». Решается вместе с
возвратом холодного края, не раньше.

Форма её известна уже сейчас: раздача в `narrow` показывает, что `Tick`
и `Opaque` идут ВСЕМ приборам, а `Packet` фильтруется через `Reads`.
Холодная буква несёт вид края без провода — значит фильтруется как
`Packet` (только читающим край), а не раздаётся всем. Имя обязано быть
носитель-независимым («край опрошен»), а не намекать на дамп таблицы:
точечный запрос по кортежу и чтение eBPF-мапы подметанием не являются.
Тип вида на ней — тот же, что вторым множителем на `Packet`, чтобы
прибор был слеп к происхождению буквы.

## Сертификация

**Предпосылки машины — в `nfqueue/preflight.rs`**, к существующим
`CAP_NET_ADMIN`/`CAP_NET_RAW`/`nfnetlink_queue` добавляются: загружен ли
`nf_conntrack`, включён ли `nf_conntrack_acct`, включён ли
`nf_conntrack_timestamp`, принят ли `NFQA_CFG_F_CONNTRACK`. Первые два
по умолчанию в ядре **выключены**, и без `acct` приборы на счётчиках
вечно видят нули при зелёной сборке. Форма ответа — существующая: причина
плюс рецепт починки.

**Девятый закон — `core/src/certify/remembering.rs`.** Восьмой занят
`replays` (§10, §12.3). Утверждение: состояние, отданное ядру, вернулось
нам следующим шагом того же потока.

```
Broken::StateLost    отдали s — вернулось не s
Broken::Clobbered    наши биты целы, чужие стёрты: RMW сломан
Invalid::NoConntrack у пакета нет NFQA_CT: судить не о чем
Invalid::AnswerNotTaken  терминал сказал, что вердикт не принят
```

Свидетель независим по коду, как требует `certify/mod.rs`: пишем через
дверь очереди (`NFNL_SUBSYS_QUEUE`), читаем через дверь дампа
(`NFNL_SUBSYS_CTNETLINK`) — разные подсистемы, разные сообщения. Разбор
`CTA_*` при этом один и тот же (закон одного чеканщика): независимость
свидетеля здесь в том, ОТКУДА взяты байты, а не в том, кем они разобраны;
второй разбор дал бы независимость ценой двух законов об одной марке.
Дамп остаётся **только свидетелем**: источником буквы на боевом пути он
не становится — иначе появилась бы третья дверь (§2). Закон звучит в `linux/examples/certify.rs` рядом с семью
остальными, на живом ядре.

`Broken::Clobbered` — подстраховка, переведённая из намерения в
проверяемое утверждение: стенд кладёт в чужие биты произвольное значение
и требует, чтобы оно пережило наш шаг.

## Что исчезает

- **`FlowTable` в фасаде** — как хранилище: прибор без состояния живёт
  одним экземпляром на все потоки. Вместе с ним уходят эвикт по простою,
  `MIN_IDLE` и фанаут тиков по ключам. Это касается §12.1 (`Region::Key`):
  боевым потребителем расслоения `FlowTable` больше не будет.
- **Уборка** (§12.5, §12.6): состояния, которого не завели, чистить не
  надо; закон смерти области объявляет conntrack своим таймаутом.

**Остаётся `idents: HashMap<FlowKey, Ident>`** — имя цели из
`ClientHello`. В 32 бита не влезает физически, в 128 бит labels тоже.
Считаем это другим предметом: имя — личность цели для человека, не
состояние автомата (расслоение `Named | Unnamed` уже это говорит).
Вопрос вынесен эксперту канона; если ответ будет «один предмет», спека
дополняется отдельным разделом.

## IPv6: предел объявлен, дорога не закрыта

IPv6 не нужен сегодня и понадобится потом. Отсюда требование не «поддержать»,
а **не увеличить число мест, где зашит IPv4**.

Сегодня их два: ковка ключа (`keyed` берёт адреса как `u32`) и разбор
четвёрки (`addressed` читает только `CTA_IP_V4_SRC/DST`). Новый код
разбирает кортежи заново — и обязан не добавить третьего.

Решение: **разбирать `CTA_IP_V6_*` сразу, ключевать — нет.** Шестнадцать
байт вместо четырёх стоят нуля, а взамен:

- отказ становится названным: не «четвёрки нет», а «кортеж IPv6, ковка
  ключа его не берёт» — причина в закрытом перечислении, как требует §7;
- будущая работа сжимается до ОДНОГО места: научить `keyed` адресу шире
  `u32`. Разбор к тому моменту уже готов и уже проверен тестами.

Пакет с IPv6-кортежем идёт как непонятый (`Opaque`) — наблюдение о нём мы
честно не ведём, но и разговоры разных целей в один ключ не сливаем.

## Куда это ведёт: ключ выбирает носитель

Записано 09.09.2026 по разговору с rcd; отдельная работа, НЕ входит в этот план.
Следующий заход начинается брейнштормом после зелёного A/B.

Потребитель описывает разные угрозы, и каждая ключуется по-своему: блок по имени —
именем, blackhole по адресу — адресом, троттлинг UDP — возможно, парой «цель × порт».
Выбор ключа принадлежит описанию угрозы, а не фреймворку (§12.1 канона называет ту же
недоделку с другой стороны: `Region::Key` пуст, `FlowTable` умеет один зашитый ключ).

Связь с этим планом прямая: **область ключа определяет носитель состояния.**

| область ключа | носитель | почему |
|---|---|---|
| разговор (поток) | conntrack | ядро ведёт запись на каждый поток; умирает вместе с ним |
| цель по адресу | nftables set/map | живёт дольше потоков, доступна правилу на wire speed |
| цель по имени | юзерспейс | ядро имён не знает |

То есть «ключевание из цепочки» и «выбор носителя» — одно решение, не два. `EdgeView`
к этому готов: он называет «слово состояния, как его хранит край», а не «марку
conntrack».

Чего одним движением не выйдет: величины у областей разные. Прибор потока читает
счётчики направлений и простой — их ведёт ядро. Прибору цели нужны агрегаты (сколько
разговоров к цели умерло без ответа за окно), которых ядро не ведёт ни в каком виде —
их придётся считать и складывать в set самим. Общей останется форма трейта, не состав.

Механика прокидывания знания вниз по пайпу в каноне уже есть и применена в одну сторону:
`Packet ⊂ Conversation ⊂ Target` и спуск `Descends`. Оператор `extract(Sni)` по смыслу
объявляет «дальше область — цель, не разговор», но типом этого не говорит; сказал бы —
ключ реакции выводился бы из цепочки сам.

**Открытые вопросы к брейншторму:** какие области ключей реально нужны продукту; нужны
ли агрегаты по цели в ядре или довольно юзерспейса; что делать со знанием, накопленным
под адресом, когда цель назвалась посреди разговора (`stood_on` в `engine/src/row.rs`
показывает, что вопрос видели).

## Швы, по которым копии расходятся молча

Оба названы явно, потому что сборка при их расхождении остаётся зелёной.

**1. Живость потока — один закон, не два.** `FlowTable` держит
юзерспейсные часы простоя (`flow_table.rs`), у conntrack часы свои.
Пока прибор был с состоянием, эвикт нёс и прибор, и запись таблицы.
После `S = ()` приборам хранилище не нужно, но **именам эвикт всё ещё
нужен**, и если он останется юзерспейсными часами рядом с ядерным
таймаутом — это двое часов об одном предмете.

Пока подписки на `NFNLGRP_CONNTRACK_DESTROY` нет (`dump.rs` умеет только
`GET`/`DUMP`), **единственным законом живости объявляется юзерспейсный
простой имён**. Ядерный таймаут вторым законом живости не вводится.
`база − CTA_TIMEOUT` в приборе тишины — не суждение о живости, а
измерение «сколько молчит»; смешивать их запрещено.

Отдельно назван вытекающий отсюда порядок отказа: имя эвиктится по
юзерспейсным часам, а состояние живёт в ядре по своим — значит поток,
переживший эвикт имени, продолжит наблюдаться, а его беда не найдёт
адресата в `fire` и потеряется молча.

**Срок жизни имени кроет горизонт клиентской активности, а не ядерный
таймаут.** Требование «имя живёт не меньше состояния» непрактично и было
бы утечкой: established-таймаут conntrack по умолчанию — сутки, и держать
каждое имя сутки значит вырастить ровно ту таблицу, ради сноса которой
всё затевается. Мерой служит та же величина, что задаёт область беды в
разделе о тишине: пока клиент напоминает о себе, имя нужно; после того
как он сдался, беда по этому потоку уже вне области. Так осиротение
запирается в тот же класс, который из области вынесен, и обе секции
сходятся в один закон.

**2. Ключ потока — одна функция, не два понятия.** Ключ таблицы имён и
кортеж conntrack обязаны быть одной и той же личностью потока: `FlowKey`
куётся из того же кортежа, каким индексируется запись ядра
(`CTA_TUPLE_ORIG`). Сегодня он куётся в юзерспейсе из разобранного
провода; после переезда две ковки одного ключа — то же расхождение при
зелёной сборке.

## Порядок работ

0. Сверка ключа: `FlowKey` из `CTA_TUPLE_ORIG` — до всего остального,
   иначе последующие шаги встанут на двух ключах.
1. Свой сокет к очереди: конфигурация, приём, вердикт; разбор `NFQA_CT`
   поверх `conntrack/wire.rs`. Переезд `Terminal`/`Serves` без изменения
   их интерфейса.
2. `CtView` в носитель, `Reads`-проекции существующим приборам.
3. Кодек марки: фаза, оттиск, тег; RMW под маской-параметром; клетка
   `Diverged`.
4. Приборы на ядерные величины: `SynDrop` (`up.packets == 0` — не пришёл
   даже `SYN+ACK`), `Retransmit` (рост `down` при неподвижном `up`),
   `Silence` (`down.bytes > 0 && up.bytes == 0`).

   **Пакеты и байты — разные величины, и путать их дорого (найдено боем).**
   Первая редакция мерила тишину цели ПАКЕТАМИ, и прибор ложно срабатывал на
   каждом открывающемся соединении: пока не пришёл `SYN+ACK`, ответных пакетов
   честно ноль — боевой прогон дал 20 ложных из 20 на чистой цели. Хуже: при
   дропе `ClientHello` ПОСЛЕ рукопожатия `SYN+ACK` приходит, `up.packets`
   становится единицей, и пакетный признак перестаёт ловить саму беду.

   Тишина цели — это **нет ДАННЫХ, а не нет пакетов**: `down.bytes > 0 &&
   up.bytes == 0` («клиент отдал данные, цель не ответила ни байтом»). Так мерил
   и старый юзерспейсный прибор — `SilenceInstrument` считает байты, — и перенос
   на счётчик пакетов был потерей различения, а не упрощением. Голое открытие
   (`down.bytes == 0`) под условие не попадает, ложного срабатывания нет.

   Пакеты остаются нужны там, где вопрос именно о пакетах: соединение, не
   состоявшееся вовсе.
5. `preflight` и девятый закон в `certify.rs`.
5.5. **Боевой регресс-гейт — до сноса чего бы то ни было.** Новый путь
   на ядерных счётчиках строится РЯДОМ с живым юзерспейсным, и на
   вантаже (`rutracker.org`, `vk.com` за реальным ТСПУ) оба пути
   работают одновременно: новый обязан поймать то же, что ловит нынешний.
   Пока гейт не зелёный, старый путь не трогается.
6. Снос `FlowTable` и юзерспейсного состояния — **только после зелёного
   гейта 5.5**.

Шаги 1–3 ломают сборку межкрейтово и едут вместе; 4–5 отделимы; 6
отделён гейтом.

**Почему гейт именно здесь.** Нынешний стенд зелёный на `rutracker`, и
стреляет он с ТИКА, из юзерспейсного `last`. Причина, по которой это
работает, — эмпирика прошлого замера: клиент повторяет `ClientHello`
примерно раз в секунду (ради этого и чинилось «повтор часы не двигает»).
То есть цель наблюдается в режиме дропа ЗАПРОСА, и новый путь обязан
поймать её на букве `Packet` — но это надо ПРОВЕРИТЬ прогоном, а не
вывести. Работающая детекция не сносится раньше, чем замена доказана
зелёной на реальном трафике.

## Риски

- **`ENOBUFS` на своей двери.** Крейт гасил его молча; у нас он обязан
  стать величиной, иначе потеря пакетов очереди читается как тишина цели —
  то есть как ложная беда.
- **Разрешение `CTA_TIMEOUT` — 1 секунда.** Окна короче секунды на нём
  не строятся; для `Retransmit` время и не нужно (он на счётчиках).
- **Оттиск в 8 бит** ошибается, если цель ответила ровно кратно 256
  пакетам между двумя нашими наблюдениями. Мы наблюдаем каждый пакет
  потока, так что случай требует нашей же слепоты на 256 пакетов подряд —
  а единственный её источник назван строкой выше. **Митигация у обоих
  рисков одна:** сделав `ENOBUFS` величиной, мы тем же движением
  закрываем алиасинг оттиска — известный провал приёма делает сравнение
  через разрыв недоверенным вместо молча-совпавшего.
- **`nf_conntrack_acct` выключен по умолчанию** — снимается preflight-ом,
  но на чужой машине это самая вероятная причина «приборы молчат».
