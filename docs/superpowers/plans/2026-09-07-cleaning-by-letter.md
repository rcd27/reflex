# Уборка по букве — план работ

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** перестать опрашивать про то, что пришло буквой, и перестать разжаловать знание в незнание по таймеру.

**Architecture:** политика снятия состояния выводится из входного алфавита. У TCP закрытие приходит буквой (`FIN`/`RST`), и шаг уже называет исход `Cursor::Ended(Closed)`. Уборка по сроку остаётся только для умерших молча и для удержания — а удержание становится названной величиной, а не следствием двух горизонтов обхода.

**Tech Stack:** Rust 2021, `engine`, `engine-nfq`.

**Spec:** `docs/superpowers/specs/2026-09-07-bounded-sweep-design.md`

## Global Constraints

* **Докблок не имеет права утверждать факт о текущем состоянии дерева.** Нельзя: счёт, адрес (`файл:строка`, `crate::модуль::имя`), «что где лежит», «раньше было так», датированные замеры. Можно: контракт, закон, смысл границы, замысел. Всё счётное и адресное — **в тело коммита**. Метки тикетов (`#320`) — не нарушение.
* **Замер — только прогоном.** Число, добытое грепом, в приёмку не идёт. Замер доказывает ровно то место, где он снят: если меряешь величину, меряй ряд, а не одно число.
* **Закрытие находки проверяется прогоном, а не заявлением:** выполни команду и вставь её ВЫВОД в отчёт.
* **Тела переносятся дословно** там, где меняются только границы и подписи.
* **`_ => …` в разборе алфавита запрещён.**
* **Базы приёмки:** `cargo test --workspace --all-features` — **96 наборов / 850 прошло / 0 провалов**; `cargo check --workspace --all-features` — **0**; `cargo doc --workspace --no-deps --all-features` — **18**; `cargo fmt --all --check` — **11** (все в `engine`/`engine-nfq`). Ниже базы не опускать.
* **`git stash`, `git checkout`, `git rebase` в рабочем дереве — НЕЛЬЗЯ.** Сравнить с другим коммитом: `git show <rev>:<путь>`, `git archive`, либо отдельный `git worktree add --detach`.
* **Репозиторий `nevod` не трогать.**

---

## Замер, из которого растёт план

Снят 07.09.2026 прогоном на `Plane` (стенд временный, снесён после замера).

| что | значение |
|---|---|
| горизонт (`BUCKETS` × корзина) | **8,6 с** |
| замолчавший разговор держится живым | **два горизонта** (17,2 с) |
| доля `Cursor::Lost` в таблице, разговор 5 с | **44,9 %** |
| доля `Cursor::Lost` в таблице, разговор 20 с | **30,0 %** |
| потолок таблицы курсоров (`CURSORS`) | 8192 |

**Главное:** первая фаза `wipe` переписывает в `Lost` и `Running`, и `Ended`. `Ended(Closed)` значит «закрылся штатно, ответ цели известен, план в силе»; `Lost` значит «состояния нет и мы не знаем, что применяли», отсюда безопасный `Act::Pass`. Таймер стирает знание, пришедшее буквой.

## Структура файлов

| файл | ответственность после плана |
|---|---|
| `engine-nfq/src/plane.rs` | уборка снимает молчащих и удерживает закрытых; знание о закрытии не разжалуется |
| `engine/src/lib.rs` | докблок `Cursor::Lost` говорит закон, а не устаревший факт о производителе |

## Порядок задач

Задача 1 несущая: она снимает порчу знания. Задача 2 даёт удержанию собственную величину. Задача 3 чинит протухший докблок. Задача 4 делает остаток уборки видимым.

---

## Задача 1: `Ended` не разжалуется в `Lost`

**Files:**
- Modify: `engine-nfq/src/plane.rs` — первая фаза `wipe`
- Test: `engine-nfq/tests/plane.rs` (дополнить)

**Interfaces:**
- Consumes: `reflex_engine::Cursor::{Running, Ended, Lost}`, `Plane::cursor_of`
- Produces: поведение — закрытый разговор остаётся `Ended` до удаления; в `Lost` переходит только тот, кто закрытия не объявлял

- [ ] **Шаг 1: снять контрольные числа**

```bash
cd /home/rcd/Workspace/reflex
cargo test --workspace --all-features 2>&1 | grep "^test result" | awk '{p+=$4; f+=$6} END {print "наборов:", NR, "прошло:", p, "провалов:", f}'
```
Ожидается `наборов: 96 прошло: 850 провалов: 0`. Число — в тело коммита.

- [ ] **Шаг 2: написать падающий тест**

Дописать в `engine-nfq/tests/plane.rs`:

```rust
/// ЗНАНИЕ, ПРИШЕДШЕЕ БУКВОЙ, НЕ СТИРАЕТСЯ ТАЙМЕРОМ.
///
/// `Ended` и `Lost` — не оттенки одного. Первое значит «закрылся штатно, ответ цели известен, план
/// в силе»; второе — «состояния нет, и мы не знаем, что применяли», оттого и безопасный `Pass`.
/// Разжаловать первое во второе по сроку значит выбросить то, что нам сказали, и отвечать на
/// хвостовой пакет незнанием там, где знание было.
#[test]
fn a_closed_talk_keeps_what_the_letter_told_us() {
    let mut plane = Plane::new(Programme::Pass, as_seen);
    let port = 44_000u16;

    feed(&mut plane, &syn(port), 0);
    feed(&mut plane, &asks(port, b"GET / HTTP/1.1\r\n\r\n"), 1_000_000);
    feed(&mut plane, &answers(port, b"HTTP/1.1 200 OK\r\n\r\n"), 2_000_000);
    feed(&mut plane, &fin(port), 3_000_000);

    let flow = flow_of(port);
    assert!(
        matches!(plane.cursor_of(flow), Cursor::Ended(_)),
        "закрытие названо буквой — курсор обязан быть Ended сразу"
    );

    // Три горизонта тиков: заведомо дольше того, за что прежде наступало разжалование.
    let horizon = reflex_engine::meter::horizon().0;
    for step in 1..=6 {
        plane.tick(Tick(3_000_000 + step * horizon / 2));
    }

    match plane.cursor_of(flow) {
        Cursor::Ended(_) => (),
        Cursor::Fresh => (), // удаление законно; разжалование — нет
        other => panic!("знание разжаловано таймером: {other:?}"),
    }
}
```

**Внимание:** имена `syn`, `asks`, `answers`, `fin`, `flow_of`, `feed`, `as_seen` взяты из шапки этого же файла. Сверь их **чтением** до написания теста; если подпись иная — правь тест под дерево, а не дерево под тест, и скажи об этом в отчёте.

- [ ] **Шаг 3: прогнать — убедиться, что падает**

```bash
cargo test -p reflex-engine-nfq --test plane a_closed_talk_keeps_what_the_letter_told_us 2>&1 | tail -20
```
Ожидается провал со словами «знание разжаловано таймером: Lost».

- [ ] **Шаг 4: развести две фазы по существу**

В `engine-nfq/src/plane.rs`, первая фаза `wipe`: сегодня в `Lost` переписываются оба варианта.

```rust
match self.cursors.get(flow) {
    Some(Cursor::Running(_)) | Some(Cursor::Ended(_)) => {
        self.cursors.insert(*flow, Cursor::Lost);
        self.seen_at.insert(*flow, now);
    }
    …
}
```

Развести:

```rust
match self.cursors.get(flow) {
    // УМЕР МОЛЧА: закрытия не объявлял, состояние снимаем — и честно говорим, что не знаем,
    // что применяли. Иначе хвостовой пакет получил бы решение, принятое заново поверх уже
    // применённого.
    Some(Cursor::Running(_)) => {
        self.cursors.insert(*flow, Cursor::Lost);
        self.seen_at.insert(*flow, now);
    }
    // ЗАКРЫЛСЯ БУКВОЙ: знание получено и не устаревает от того, что прошло время. Удержание
    // отмеряется, но содержимое не подменяется.
    Some(Cursor::Ended(_)) => {
        self.seen_at.insert(*flow, now);
    }
    …
}
```

**Тело второй фазы переносится дословно** — она удаляет и не различала варианты.

- [ ] **Шаг 5: прогнать**

```bash
cargo test -p reflex-engine-nfq --test plane 2>&1 | grep "^test result"
cargo test --workspace --all-features 2>&1 | grep "^test result" | awk '{p+=$4; f+=$6} END {print "наборов:", NR, "прошло:", p, "провалов:", f}'
cargo check --workspace --all-features 2>&1 | grep -c "^warning"
cargo fmt --all --check 2>&1 | grep -c "^Diff"
```
Провалов **0**, тестов не меньше **850**; `check` **0**; `fmt` **11**.

- [ ] **Шаг 6: коммит**

Сообщение называет: чем `Ended` отличается от `Lost` по существу и почему разжалование по сроку есть потеря знания.

---

## Задача 2: удержание — названная величина

**Files:**
- Modify: `engine-nfq/src/plane.rs` — константа удержания и её употребление во второй фазе `wipe`
- Test: `engine-nfq/tests/plane.rs` (дополнить)

**Interfaces:**
- Produces: `pub const RETENTION_HORIZONS: u64` (имя на усмотрение исполнителя, смысл — сколько горизонтов помним закрытый разговор)

- [ ] **Шаг 1: написать падающий тест**

```rust
/// УДЕРЖАНИЕ — ВЕЛИЧИНА, А НЕ ПОБОЧНЫЙ ЭФФЕКТ ОБХОДА.
///
/// Закрытый разговор помнится ровно затем, чтобы хвостовой пакет (последний `ACK`,
/// ретрансмиссия) не получил ответ «не видели» про то, по чему выносился вердикт. Сколько
/// именно — величина со своим доводом, а не то, что вышло из двух проходов уборки.
#[test]
fn retention_is_a_named_span_not_a_side_effect() {
    let mut plane = Plane::new(Programme::Pass, as_seen);
    let port = 44_100u16;
    feed(&mut plane, &syn(port), 0);
    feed(&mut plane, &fin(port), 1_000_000);

    let flow = flow_of(port);
    let horizon = reflex_engine::meter::horizon().0;
    let retention = reflex_engine_nfq::plane::RETENTION_HORIZONS * horizon;

    // На середине удержания разговор ещё помнится.
    plane.tick(Tick(1_000_000 + retention / 2));
    assert!(
        !matches!(plane.cursor_of(flow), Cursor::Fresh),
        "внутри удержания закрытый разговор обязан помниться"
    );

    // За удержанием — забыт.
    plane.tick(Tick(1_000_000 + retention + horizon));
    plane.tick(Tick(1_000_000 + retention + 2 * horizon));
    assert!(
        matches!(plane.cursor_of(flow), Cursor::Fresh),
        "за удержанием память о закрытом разговоре обязана уйти"
    );
}
```

- [ ] **Шаг 2: прогнать — убедиться, что не собирается**

```bash
cargo test -p reflex-engine-nfq --test plane retention_is_a_named_span 2>&1 | tail -20
```
Ожидается: `RETENTION_HORIZONS` не существует.

- [ ] **Шаг 3: назвать величину**

В `engine-nfq/src/plane.rs`, рядом с прочими константами:

```rust
/// СКОЛЬКО ГОРИЗОНТОВ ПОМНИМ ЗАКРЫТЫЙ РАЗГОВОР.
///
/// Память о закрытом держится не ради него самого, а ради ХВОСТА: последний `ACK` и
/// ретрансмиссия приходят уже после прощания, и ответить им «не видели» значило бы соврать про
/// разговор, по которому мы выносили вердикт.
///
/// Величина названа здесь, а не выведена из числа проходов уборки: срок жизни хвоста есть
/// свойство транспорта, а частота уборки — наше удобство, и связывать их значило бы менять
/// первое, настраивая второе.
pub const RETENTION_HORIZONS: u64 = 2;
```

Вторая фаза `wipe` начинает отмерять удержание этой величиной, а не тем, что запись пережила ещё один проход.

- [ ] **Шаг 4: прогнать**

```bash
cargo test -p reflex-engine-nfq --test plane 2>&1 | grep "^test result"
cargo test --workspace --all-features 2>&1 | grep "^test result" | awk '{p+=$4; f+=$6} END {print "наборов:", NR, "прошло:", p, "провалов:", f}'
cargo doc --workspace --no-deps --all-features 2>&1 | grep -c "^warning"
cargo fmt --all --check 2>&1 | grep -c "^Diff"
```

- [ ] **Шаг 5: коммит**

Сообщение называет: почему срок хвоста — свойство транспорта, а не следствие частоты уборки.

---

## Задача 3: докблок `Cursor::Lost` протух

**Files:**
- Modify: `engine/src/lib.rs` — докблок варианта `Cursor::Lost`

**Interfaces:** нет — правка прозы.

- [ ] **Шаг 1: прочитать и убедиться**

```bash
grep -n "Производителя в плоскости сегодня НЕТ" engine/src/lib.rs
```
Докблок утверждает, что производителя у `Lost` в плоскости нет и вариант «держится под настоящего». Уборка его производит.

- [ ] **Шаг 2: сказать законом**

Заменить утверждение о состоянии дерева контрактом. Например:

```rust
    /// СОСТОЯНИЕ ПОТЕРЯНО, И МЫ НЕ ЗНАЕМ ЧТО ПРИМЕНЯЛИ. Не то же, что `Ended`, и разница не в
    /// оттенке: страта могла уже уйти с приветствием, и принять решение заново значило бы
    /// применить полторы. Отсюда `Act::Pass` — единственное безопасное при неизвестном прошлом.
    ///
    /// Сюда приходит разговор, о конце которого НЕ БЫЛО СКАЗАНО: он замолчал, и состояние снято
    /// по сроку. Закрывшийся буквой сюда не приходит — у него исход известен, и подменять знание
    /// незнанием значило бы терять то, что уже сказано.
    Lost,
```

Формулировка на усмотрение исполнителя; требование одно: **ни утверждения о том, есть ли производитель, ни счёта, ни адреса**.

- [ ] **Шаг 3: прогнать**

```bash
cargo doc --workspace --no-deps --all-features 2>&1 | grep -c "^warning"
cargo fmt --all --check 2>&1 | grep -c "^Diff"
grep -c "Производителя в плоскости сегодня НЕТ" engine/src/lib.rs
```
`doc` **18**, `fmt` **11**, последний греп — **0**.

- [ ] **Шаг 4: коммит**

---

## Задача 4: остаток уборки виден

**Files:**
- Modify: `engine-nfq/src/plane.rs` — `wipe` отдаёт остаток
- Test: `engine-nfq/tests/plane.rs` (дополнить)

**Interfaces:**
- Produces: `Plane::backlog() -> usize` — сколько записей осталось неосмотренными на момент последнего прохода

- [ ] **Шаг 1: написать падающий тест**

```rust
/// ЗАВАЛ ВИДЕН ВЕЛИЧИНОЙ, А НЕ ОПОЗДАНИЕМ.
///
/// Проход осматривает не больше бюджета. Если записей больше, остаток копится — и узнать об этом
/// человек обязан числом заранее, а не по тому, что тревоги начали опаздывать.
#[test]
fn the_backlog_is_a_number_not_a_surprise() {
    let mut plane = Plane::new(Programme::Pass, as_seen);
    let over_budget = reflex_engine_nfq::plane::SWEEP_BUDGET + 50;

    for i in 0..over_budget {
        let port = 45_000 + i as u16;
        feed(&mut plane, &syn(port), i as u64 * 1_000);
    }

    plane.tick(Tick(1_000_000));
    assert!(
        plane.backlog() > 0,
        "записей больше бюджета — остаток обязан быть назван"
    );
}
```

- [ ] **Шаг 2: прогнать — убедиться, что не собирается**

```bash
cargo test -p reflex-engine-nfq --test plane the_backlog_is_a_number 2>&1 | tail -20
```
Ожидается: метода `backlog` не существует.

- [ ] **Шаг 3: назвать остаток**

В `engine-nfq/src/plane.rs` завести поле рядом с прочими счётчиками уборки:

```rust
    /// Сколько записей осталось за пределами бюджета на последнем проходе.
    backlog: usize,
```

инициализировать нулём в `Plane::new`, а в `wipe` — после того как проход собрал `looked`, — положить остаток:

```rust
                self.backlog = self.seen_at.len().saturating_sub(looked.len());
```

Наружу отдаётся методом:

```rust
    /// СКОЛЬКО ЗАПИСЕЙ ОСТАЛОСЬ НЕОСМОТРЕННЫМИ на последнем проходе уборки.
    ///
    /// Растущий остаток значит, что круг удлиняется: тревоги ещё не опаздывают, но начнут. Это
    /// показание, а не счётчик работы, — оно говорит о ЗАПАСЕ, а не о сделанном.
    pub fn backlog(&self) -> usize {
        self.backlog
    }
```

- [ ] **Шаг 4: прогнать**

```bash
cargo test -p reflex-engine-nfq --test plane 2>&1 | grep "^test result"
cargo test --workspace --all-features 2>&1 | grep "^test result" | awk '{p+=$4; f+=$6} END {print "наборов:", NR, "прошло:", p, "провалов:", f}'
cargo check --workspace --all-features 2>&1 | grep -c "^warning"
cargo fmt --all --check 2>&1 | grep -c "^Diff"
```

- [ ] **Шаг 5: коммит**

---

## Приёмка плана

| что | до | после |
|---|---|---|
| `Ended` разжалуется в `Lost` по сроку | **да** | **нет** (тест) |
| доля `Lost` в таблице при разговоре 5 с | **44,9 %** | замерить тем же стендом; ожидается падение |
| удержание | следствие двух проходов уборки | **названная величина с доводом** |
| докблок `Cursor::Lost` | утверждает, что производителя нет | **говорит закон** |
| остаток уборки | не виден | **`backlog()`** |
| тестов в воркспейсе | 850 | не меньше |

## Что этот план НЕ делает

* **Ограниченный обход в фундаменте** (§7 спеки) — резервный путь для потребителей, которым закрытие буквой не приходит. После этого плана станет видно, велик ли он вообще.
* **Закон ёмкости** — «место кончилось, выселяй старейшего», написан не меньше пяти раз; отдельная работа.
* **UDP и QUIC.** У них закрытие не приходит (у QUIC приходит, но зашифровано), и срок для них — главный путь. Сегодня в дереве состояния по датаграммам нет вовсе (`watch_datagram` его не держит), и работать не с чем.
* **Ограничение доставки буквы `Tick`.**
* **Вывод политики типом.** Спека (§8.3) оставила открытым, чем выражается «политика выводится из источника» — типом (источник объявляет, даёт ли он закрытие буквой) или соглашением. Этот план чинит одного потребителя, у которого источник известен по построению, и вопроса не решает. Решать его стоит тогда, когда появится второй источник с закрытием буквой, — иначе тип будет выведен из одного примера.
* **Миграция потребителя.**
