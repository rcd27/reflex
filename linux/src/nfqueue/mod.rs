//! Очередь ядра. Правило netfilter потребитель ставит СНАРУЖИ (скриптами стенда), а из очереди
//! берёт [`Answer`] — ОДИН алфавит вердикта на четыре слова очереди: второй набор тех же слов
//! ничем не проверяем, и разойтись с первым он может молча.
//!
//! Правило хозяина: код без живого потребителя либо уходит, либо обзаводится примером, который его
//! УПОТРЕБЛЯЕТ, — пример, держащий сам себя, потребителем не считается. Боевой путь ходит через
//! [`crate::queue::QueueSocket`].

mod backend;
pub mod preflight;
mod terminal;

pub use backend::{NfqueueBackend, Waited};
pub use terminal::{Answer, NotTaken, Queued};
// Только внутри крейта: `millis_until` — закон округления остатка до `poll`, общий с
// `queue::terminal` (второй бэкенд на своём netlink-сокете, тот же предмет).
pub(crate) use terminal::millis_until;
