[package]
name = "watch-two-wires"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
description = "Use-case: две цепочки рядом — разные транспорты, одна нить, один поток показаний"

[dependencies]
reflex = { path = "../../reflex" }

[[bin]]
name = "watch-two-wires"
path = "src/main.rs"
