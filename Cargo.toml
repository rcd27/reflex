[workspace]
resolver = "2"
members = [
    "core",
    "runtime",
    "linux",
    "linux-common",
    "os",
    # ПРИБОРЫ — ПАСПОРТ РАЗЛИЧАЮЩЕЙ СПОСОБНОСТИ (приехал 06.09.2026 из продукта, #326).
    #
    # Крейт зависел ровно от `reflex-core` и ни от чего продуктового — то есть был фундаментным по
    # построению и лежал не там, где живёт. Что он знает: сброс, тишина, троттлинг, повтор без
    # ответа, судьба соединения, доверие к цели, словарь провода. Всё это понятия ВСЯКОГО движка в
    # разрыве, а не нашего продукта.
    #
    # История файлов осталась в суперпроекте (`rcd27/nevod`, до `7985d309` включительно): переезд
    # между репозиториями её не переносит, и притворяться, будто переносит, незачем.
    "instrument",
    # ДВИЖОК В РАЗРЫВЕ (приехал 06.09.2026 из продукта, #326) — то, ради чего затевался перенос.
    #
    # `engine` — ядро: курсор разговора, счёт байтов по окну, момент раскрытия личности, выпуск из
    # горячего пути, И (задача 12½, 10.09.2026) разбор провода (`parse`) с таблицей разговоров
    # (`talk`).
    #
    # СОСЕДА `engine-nfq` БОЛЬШЕ НЕТ (снесён 10.09.2026 по решению хозяина). Он держал `Plane` —
    # ЧЕТВЁРТЫЙ дом состояния, где состояние машины живёт в ДРАЙВЕРЕ девятнадцатью картами. Три
    # дома, которые фреймворк признаёт, — сам прибор (значение, возвращённое шагом), таблица по
    # ключу разговора, марка ядра; четвёртый был тем, ОТ ЧЕГО ушли. Оттого крейт был и
    # непереиспользуем: не оттого, что мал, а оттого, что хранил отвергнутую модель. Примера к нему
    # написать было не на чем — пример показывал бы старый способ употребления нового движка.
    #
    # Чистое из него уехало ДО сноса и живо: `parse`/`talk` в этом крейте (задача 12½ — они не
    # звали netlink и не знали очереди, но жили в крейте, безусловно зависящем от Linux, и это одно
    # валило кросс-сборку фасада на Windows), законы разбора — в `engine/tests/`, закон согласия
    # проводного и ядерного ключа — в `linux/tests/flow_identity.rs`.
    #
    # РАЗРЕЗ БЫЛ СДЕЛАН ДО ПЕРЕЕЗДА: приборы, читавшие счётчики НАШЕЙ схемы правил и `/proc`
    # конкретного чужого бинаря, остались наверху (`nevod2-runtime::kernel`). Взять папку целиком
    # значило бы увезти в общий фундамент адаптер к zapret и имя нашей таблицы.
    #
    # История файлов осталась в суперпроекте (`rcd27/nevod`, по `50afa25c` включительно).
    "engine",
    # ФАСАД — единственная дверь фреймворка (`engine(X).from…`). Прячет всё выше.
    "reflex",
    # НОСИТЕЛЬ WINDIVERT (Task 12, 10.09.2026) — СВИДЕТЕЛЬ ФОРМЫ, не работы: живьём здесь ничто не
    # бежит (нет Windows), и это сказано вслух в докблоке крейта. Член воркспейса НАРОЧНО, не
    # исключён: на Linux он собирается ПУСТЫМ (`#[cfg(windows)]` на всём, что зовёт настоящий FFI —
    # `windivert/src/carrier.rs`), и `cargo build --workspace` видит эту пустоту, а не молчание
    # исключённого крейта. Годность формы для Windows проверяет отдельно `cargo check -p
    # reflex-windivert --target x86_64-pc-windows-msvc` — не эта сборка.
    #
    # ЗАВИСИТ ОТ `reflex-core`, НЕ ОТ `reflex` — ЭТИМ КРЕЙТОМ НЕ ТРОНУТО. Дом трейта `IntoCarrier`
    # (`reflex`) на момент задачи 12 не проходил кросс-проверку на Windows по причине, к WinDivert
    # отношения не имеющей (`reflex` безусловно зависел от `reflex-engine-nfq` → `reflex-linux` →
    # внешний крейт `nfq`, который не собирается под `windows-msvc`) — докблок `windivert/src/lib.rs`
    # называет это цитатой отказа. Задача 12½ (10.09.2026) отвязала фасад (`parse`/`talk` переехали
    # в портативный `reflex-engine`, `cargo check -p reflex --target x86_64-pc-windows-msvc` теперь
    # `Finished`) — связывание (`impl IntoCarrier for WinDivert`) стало возможным, но НЕ сделано
    # ЗДЕСЬ: правка потянула бы `windivert/src/lib.rs` и `Cargo.toml`, а крейт занят ревью.
    "windivert",
    # ПРИМЕРЫ — эталон использования, один use-case на подпапку. Тест готовности API: если
    # примеру нужно что-то, кроме `reflex`, — дефект фасада, а не примера.
    "examples/detect-silent-drop",
    "examples/detect-syn-drop",
    "examples/detect-dns-poisoning",
    "examples/sever-silent-drop",
    "examples/bring-your-own-detector",
    "examples/certify-replays",
    "examples/replay-recording",
]
# linux-ebpf is NOT a workspace member — it compiles to BPF target separately
