[workspace]
resolver = "2"
members = [
    "core",
    "runtime",
    "linux",
    "linux-common",
    "os",
    "examples/rst-injection",
]
# linux-ebpf is NOT a workspace member — it compiles to BPF target separately
