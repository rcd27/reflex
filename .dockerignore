# Контекст docker-сборки — корень воркспейса (примеры path-зависят от крейтов).
# Тяжёлое и невоспроизводимое — вон, чтобы сборка была герметичной и быстрой.
target
**/target
.git
linux-ebpf/target
out
**/*.snapshot
.idea
.remember
